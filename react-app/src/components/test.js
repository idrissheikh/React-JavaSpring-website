import React, { Component } from "react";

class test extends Component {
  render() {
    return (
      <div>
        <h2>I am test with title: {this.props.title}</h2>
      </div>
    );
  }
}

export default test;
